# 需求索引

公共库需求必须说明至少两个实际消费场景，或解释为何该契约必须位于跨服务边界。每个需求使用独立 Markdown 文件，例如 `YYYYMMDD-short-name.md`。

## 模板

```markdown
# 需求标题

## 背景与消费方

## 公共能力与非目标

## API/序列化契约

## 兼容性与版本策略

## 安全和依赖影响

## 验收条件

- [ ] 可验证结果

## 测试与消费方验证

## 发布、迁移与回滚
```

如果只有单一领域使用，默认在领域仓库实现；不要为了代码复用过早扩大公共契约。

